{"headerFile":"/**\n * Smart-City Real-Time Emergency Vehicle Routing System\n * Header File: emergency_routing.h\n * \n * Target Constraints:\n * - Intersections: > 5,000 nodes\n * - Roads: > 10,000 edges\n * - Query Latency: < 200 ms\n * - Data Structures: Adjacency List via HashMap, Min-Heap Priority Queue, Visited HashSet\n */\n\n#ifndef EMERGENCY_ROUTING_H\n#define EMERGENCY_ROUTING_H\n\n#include <stdio.h>\n#include <stdlib.h>\n#include <stdbool.h>\n#include <string.h>\n#include <math.h>\n#include <time.h>\n\n#define MAX_NODES_DEFAULT 6000\n#define HASH_TABLE_CAPACITY 12289 // Prime number for hash bucket size\n#define INF_WEIGHT 1e9\n\n// Vehicle Types\ntypedef enum {\n    VEHICLE_AMBULANCE,\n    VEHICLE_FIRE_ENGINE\n} VehicleType;\n\n// Edge in Adjacency List\ntypedef struct Edge {\n    int src;\n    int dest;\n    double base_distance;      // In kilometers or meters\n    double traffic_multiplier; // 1.0 = Normal, 2.0 = Heavy, 10.0 = Blocked/Accident\n    double effective_weight;   // base_distance * traffic_multiplier\n    struct Edge* next;\n} Edge;\n\n// HashMap Node Entry for Graph Adjacency List\ntypedef struct NodeHashEntry {\n    int node_id;\n    char name[64];\n    double x_coord;\n    double y_coord;\n    Edge* head_edge; // Linked list of outgoing edges\n    struct NodeHashEntry* next_bucket_entry;\n} NodeHashEntry;\n\n// Graph represented using a Hash Map of Adjacency Lists\ntypedef struct {\n    NodeHashEntry* buckets[HASH_TABLE_CAPACITY];\n    int total_nodes;\n    int total_edges;\n} GraphHashMap;\n\n// HashSet for O(1) visited-node tracking\ntypedef struct HashSetNode {\n    int key;\n    struct HashSetNode* next;\n} HashSetNode;\n\ntypedef struct {\n    HashSetNode* buckets[HASH_TABLE_CAPACITY];\n    int size;\n} HashSet;\n\n// Min-Heap Priority Queue Node\ntypedef struct {\n    int node_id;\n    double dist;\n} MinHeapNode;\n\n// Min-Heap Priority Queue\ntypedef struct {\n    MinHeapNode* array;\n    int* pos; // Map node_id -> position in heap for O(log V) decreaseKey\n    int capacity;\n    int size;\n} MinHeap;\n\n// Route Result Structure\ntypedef struct {\n    int* path;\n    int path_length;\n    double total_distance;\n    double total_time_minutes;\n    double execution_time_ms;\n    int nodes_visited_count;\n    int heap_operations_count;\n} RouteResult;\n\n// Multi-dispatch query result\ntypedef struct {\n    int best_dispatch_node;\n    int target_incident_node;\n    RouteResult route;\n} MultiDispatchResult;\n\n// Function Prototypes\n\n// Graph Operations\nGraphHashMap* create_graph(void);\nvoid free_graph(GraphHashMap* graph);\nbool add_node(GraphHashMap* graph, int node_id, const char* name, double x, double y);\nbool add_edge(GraphHashMap* graph, int src, int dest, double base_distance, double traffic_multiplier);\nEdge* find_edge(GraphHashMap* graph, int src, int dest);\n\n// Dynamic Traffic Management - O(1) dynamic update\nbool update_traffic_condition(GraphHashMap* graph, int src, int dest, double new_multiplier);\n\n// HashSet Operations\nHashSet* create_hashset(void);\nvoid free_hashset(HashSet* set);\nbool hashset_insert(HashSet* set, int key);\nbool hashset_contains(HashSet* set, int key);\nvoid hashset_clear(HashSet* set);\n\n// MinHeap Operations\nMinHeap* create_minheap(int capacity);\nvoid free_minheap(MinHeap* heap);\nvoid minheap_insert(MinHeap* heap, int node_id, double dist);\nMinHeapNode minheap_extract_min(MinHeap* heap);\nvoid minheap_decrease_key(MinHeap* heap, int node_id, double new_dist);\nbool is_minheap_empty(MinHeap* heap);\nbool minheap_contains(MinHeap* heap, int node_id);\n\n// Routing Algorithms\nRouteResult dijkstra_shortest_path(GraphHashMap* graph, int start_node, int end_node, VehicleType vehicle);\nMultiDispatchResult find_nearest_emergency_dispatch(GraphHashMap* graph, int* dispatch_nodes, int dispatch_count, int incident_node, VehicleType vehicle);\n\n// Benchmark & Demonstration Suite\nvoid benchmark_5000_nodes_test(void);\nvoid print_data_structure_comparison(void);\n\n#endif // EMERGENCY_ROUTING_H\n","sourceFile":"/**\n * Smart-City Real-Time Emergency Vehicle Routing System\n * Source File: emergency_routing.c\n * \n * Implements:\n * 1. HashMap-based Adjacency List graph storage (O(1) average lookup for 5,000+ nodes, 10,000+ edges)\n * 2. HashSet for O(1) visited-node tracking\n * 3. Min-Heap Priority Queue for Dijkstra's Algorithm - O((V + E) log V) complexity\n * 4. Dynamic Traffic Management Module - O(1) road condition updates\n * 5. Latency Benchmark Suite (< 200 ms execution guarantee)\n * 6. One-to-One and Multi-Location Emergency Dispatch Queries\n */\n\n#include \"emergency_routing.h\"\n\n// Hash function for node IDs\nstatic inline int hash_node_id(int key) {\n    int h = key % HASH_TABLE_CAPACITY;\n    return h < 0 ? h + HASH_TABLE_CAPACITY : h;\n}\n\n// ----------------------------------------------------\n// GRAPH HASH MAP IMPLEMENTATION\n// ----------------------------------------------------\n\nGraphHashMap* create_graph(void) {\n    GraphHashMap* graph = (GraphHashMap*)calloc(1, sizeof(GraphHashMap));\n    if (!graph) {\n        fprintf(stderr, \"Error: Memory allocation failed for GraphHashMap.\\n\");\n        exit(EXIT_FAILURE);\n    }\n    graph->total_nodes = 0;\n    graph->total_edges = 0;\n    return graph;\n}\n\nvoid free_graph(GraphHashMap* graph) {\n    if (!graph) return;\n    for (int i = 0; i < HASH_TABLE_CAPACITY; i++) {\n        NodeHashEntry* entry = graph->buckets[i];\n        while (entry) {\n            NodeHashEntry* temp_entry = entry;\n            // Free outgoing edges\n            Edge* edge = entry->head_edge;\n            while (edge) {\n                Edge* temp_edge = edge;\n                edge = edge->next;\n                free(temp_edge);\n            }\n            entry = entry->next_bucket_entry;\n            free(temp_entry);\n        }\n    }\n    free(graph);\n}\n\nbool add_node(GraphHashMap* graph, int node_id, const char* name, double x, double y) {\n    if (!graph) return false;\n    int index = hash_node_id(node_id);\n    NodeHashEntry* curr = graph->buckets[index];\n    while (curr) {\n        if (curr->node_id == node_id) {\n            // Update node metadata\n            if (name) strncpy(curr->name, name, sizeof(curr->name) - 1);\n            curr->x_coord = x;\n            curr->y_coord = y;\n            return true;\n        }\n        curr = curr->next_bucket_entry;\n    }\n    \n    // Create new node entry\n    NodeHashEntry* new_node = (NodeHashEntry*)calloc(1, sizeof(NodeHashEntry));\n    new_node->node_id = node_id;\n    if (name) {\n        strncpy(new_node->name, name, sizeof(new_node->name) - 1);\n    } else {\n        snprintf(new_node->name, sizeof(new_node->name), \"Intersection_%d\", node_id);\n    }\n    new_node->x_coord = x;\n    new_node->y_coord = y;\n    new_node->head_edge = NULL;\n    new_node->next_bucket_entry = graph->buckets[index];\n    graph->buckets[index] = new_node;\n    graph->total_nodes++;\n    return true;\n}\n\nbool add_edge(GraphHashMap* graph, int src, int dest, double base_distance, double traffic_multiplier) {\n    if (!graph) return false;\n    add_node(graph, src, NULL, 0, 0);\n    add_node(graph, dest, NULL, 0, 0);\n\n    int index = hash_node_id(src);\n    NodeHashEntry* curr = graph->buckets[index];\n    while (curr && curr->node_id != src) {\n        curr = curr->next_bucket_entry;\n    }\n    if (!curr) return false;\n\n    // Check if edge already exists\n    Edge* edge = curr->head_edge;\n    while (edge) {\n        if (edge->dest == dest) {\n            edge->base_distance = base_distance;\n            edge->traffic_multiplier = traffic_multiplier;\n            edge->effective_weight = base_distance * traffic_multiplier;\n            return true;\n        }\n        edge = edge->next;\n    }\n\n    // Add new outgoing edge\n    Edge* new_edge = (Edge*)calloc(1, sizeof(Edge));\n    new_edge->src = src;\n    new_edge->dest = dest;\n    new_edge->base_distance = base_distance;\n    new_edge->traffic_multiplier = traffic_multiplier;\n    new_edge->effective_weight = base_distance * traffic_multiplier;\n    new_edge->next = curr->head_edge;\n    curr->head_edge = new_edge;\n    graph->total_edges++;\n    return true;\n}\n\nEdge* find_edge(GraphHashMap* graph, int src, int dest) {\n    if (!graph) return NULL;\n    int index = hash_node_id(src);\n    NodeHashEntry* curr = graph->buckets[index];\n    while (curr && curr->node_id != src) {\n        curr = curr->next_bucket_entry;\n    }\n    if (!curr) return NULL;\n    Edge* edge = curr->head_edge;\n    while (edge) {\n        if (edge->dest == dest) return edge;\n        edge = edge->next;\n    }\n    return NULL;\n}\n\n// ----------------------------------------------------\n// DYNAMIC TRAFFIC MANAGEMENT MODULE - O(1) UPDATES\n// ----------------------------------------------------\n\nbool update_traffic_condition(GraphHashMap* graph, int src, int dest, double new_multiplier) {\n    Edge* edge = find_edge(graph, src, dest);\n    if (!edge) return false;\n    edge->traffic_multiplier = new_multiplier;\n    edge->effective_weight = edge->base_distance * new_multiplier;\n    return true;\n}\n\n// ----------------------------------------------------\n// HASHSET FOR O(1) VISITED-NODE TRACKING\n// ----------------------------------------------------\n\nHashSet* create_hashset(void) {\n    HashSet* set = (HashSet*)calloc(1, sizeof(HashSet));\n    return set;\n}\n\nvoid free_hashset(HashSet* set) {\n    if (!set) return;\n    hashset_clear(set);\n    free(set);\n}\n\nvoid hashset_clear(HashSet* set) {\n    if (!set) return;\n    for (int i = 0; i < HASH_TABLE_CAPACITY; i++) {\n        HashSetNode* curr = set->buckets[i];\n        while (curr) {\n            HashSetNode* temp = curr;\n            curr = curr->next;\n            free(temp);\n        }\n        set->buckets[i] = NULL;\n    }\n    set->size = 0;\n}\n\nbool hashset_insert(HashSet* set, int key) {\n    if (!set) return false;\n    int index = hash_node_id(key);\n    HashSetNode* curr = set->buckets[index];\n    while (curr) {\n        if (curr->key == key) return false; // Already present\n        curr = curr->next;\n    }\n    HashSetNode* node = (HashSetNode*)malloc(sizeof(HashSetNode));\n    node->key = key;\n    node->next = set->buckets[index];\n    set->buckets[index] = node;\n    set->size++;\n    return true;\n}\n\nbool hashset_contains(HashSet* set, int key) {\n    if (!set) return false;\n    int index = hash_node_id(key);\n    HashSetNode* curr = set->buckets[index];\n    while (curr) {\n        if (curr->key == key) return true;\n        curr = curr->next;\n    }\n    return false;\n}\n\n// ----------------------------------------------------\n// MIN-HEAP PRIORITY QUEUE IMPLEMENTATION\n// ----------------------------------------------------\n\nMinHeap* create_minheap(int capacity) {\n    MinHeap* heap = (MinHeap*)malloc(sizeof(MinHeap));\n    heap->array = (MinHeapNode*)malloc(sizeof(MinHeapNode) * capacity);\n    heap->pos = (int*)malloc(sizeof(int) * capacity);\n    heap->capacity = capacity;\n    heap->size = 0;\n    for (int i = 0; i < capacity; i++) {\n        heap->pos[i] = -1;\n    }\n    return heap;\n}\n\nvoid free_minheap(MinHeap* heap) {\n    if (!heap) return;\n    free(heap->array);\n    free(heap->pos);\n    free(heap);\n}\n\nstatic void swap_minheap_node(MinHeapNode* a, MinHeapNode* b, int* pos, MinHeapNode* heap_arr) {\n    pos[a->node_id] = (int)(b - heap_arr);\n    pos[b->node_id] = (int)(a - heap_arr);\n    MinHeapNode temp = *a;\n    *a = *b;\n    *b = temp;\n}\n\nstatic void min_heapify(MinHeap* heap, int idx) {\n    int smallest = idx;\n    int left = 2 * idx + 1;\n    int right = 2 * idx + 2;\n\n    if (left < heap->size && heap->array[left].dist < heap->array[smallest].dist) {\n        smallest = left;\n    }\n    if (right < heap->size && heap->array[right].dist < heap->array[smallest].dist) {\n        smallest = right;\n    }\n\n    if (smallest != idx) {\n        swap_minheap_node(&heap->array[smallest], &heap->array[idx], heap->pos, heap->array);\n        min_heapify(heap, smallest);\n    }\n}\n\nbool is_minheap_empty(MinHeap* heap) {\n    return heap->size == 0;\n}\n\nbool minheap_contains(MinHeap* heap, int node_id) {\n    if (node_id < 0 || node_id >= heap->capacity) return false;\n    return heap->pos[node_id] != -1;\n}\n\nvoid minheap_insert(MinHeap* heap, int node_id, double dist) {\n    if (heap->size >= heap->capacity) return;\n    int i = heap->size;\n    heap->size++;\n    heap->array[i].node_id = node_id;\n    heap->array[i].dist = dist;\n    heap->pos[node_id] = i;\n\n    while (i > 0 && heap->array[i].dist < heap->array[(i - 1) / 2].dist) {\n        swap_minheap_node(&heap->array[i], &heap->array[(i - 1) / 2], heap->pos, heap->array);\n        i = (i - 1) / 2;\n    }\n}\n\nMinHeapNode minheap_extract_min(MinHeap* heap) {\n    if (is_minheap_empty(heap)) {\n        MinHeapNode dummy = {-1, INF_WEIGHT};\n        return dummy;\n    }\n    MinHeapNode root = heap->array[0];\n    MinHeapNode last = heap->array[heap->size - 1];\n    heap->array[0] = last;\n    heap->pos[root.node_id] = -1;\n    heap->pos[last.node_id] = 0;\n    heap->size--;\n    min_heapify(heap, 0);\n    return root;\n}\n\nvoid minheap_decrease_key(MinHeap* heap, int node_id, double new_dist) {\n    int i = heap->pos[node_id];\n    if (i == -1) return;\n    heap->array[i].dist = new_dist;\n    while (i > 0 && heap->array[i].dist < heap->array[(i - 1) / 2].dist) {\n        swap_minheap_node(&heap->array[i], &heap->array[(i - 1) / 2], heap->pos, heap->array);\n        i = (i - 1) / 2;\n    }\n}\n\n// ----------------------------------------------------\n// DIJKSTRA'S ALGORITHM WITH MIN-HEAP PRIORITY QUEUE\n// ----------------------------------------------------\n\nRouteResult dijkstra_shortest_path(GraphHashMap* graph, int start_node, int end_node, VehicleType vehicle) {\n    RouteResult result;\n    memset(&result, 0, sizeof(RouteResult));\n    clock_t start_clk = clock();\n\n    if (!graph || graph->total_nodes == 0) return result;\n\n    int max_node_id = MAX_NODES_DEFAULT;\n    double* dist = (double*)malloc(sizeof(double) * max_node_id);\n    int* parent = (int*)malloc(sizeof(int) * max_node_id);\n    for (int i = 0; i < max_node_id; i++) {\n        dist[i] = INF_WEIGHT;\n        parent[i] = -1;\n    }\n\n    HashSet* visited = create_hashset();\n    MinHeap* heap = create_minheap(max_node_id);\n\n    dist[start_node] = 0.0;\n    minheap_insert(heap, start_node, 0.0);\n    result.heap_operations_count++;\n\n    // Vehicle speed modifier (Fire engines slightly larger/slower in heavy traffic, ambulances get priority siren factor)\n    double siren_factor = (vehicle == VEHICLE_AMBULANCE) ? 0.85 : 0.95;\n\n    while (!is_minheap_empty(heap)) {\n        MinHeapNode min_node = minheap_extract_min(heap);\n        result.heap_operations_count++;\n        int u = min_node.node_id;\n\n        if (hashset_contains(visited, u)) continue;\n        hashset_insert(visited, u);\n        result.nodes_visited_count++;\n\n        if (u == end_node) break; // Reached target destination\n\n        // Fetch outgoing edges from HashMap\n        int index = hash_node_id(u);\n        NodeHashEntry* curr = graph->buckets[index];\n        while (curr && curr->node_id != u) {\n            curr = curr->next_bucket_entry;\n        }\n\n        if (curr) {\n            Edge* edge = curr->head_edge;\n            while (edge) {\n                int v = edge->dest;\n                double weight = edge->effective_weight * siren_factor;\n\n                if (!hashset_contains(visited, v) && dist[u] + weight < dist[v]) {\n                    dist[v] = dist[u] + weight;\n                    parent[v] = u;\n\n                    if (minheap_contains(heap, v)) {\n                        minheap_decrease_key(heap, v, dist[v]);\n                        result.heap_operations_count++;\n                    } else {\n                        minheap_insert(heap, v, dist[v]);\n                        result.heap_operations_count++;\n                    }\n                }\n                edge = edge->next;\n            }\n        }\n    }\n\n    // Reconstruct path\n    if (dist[end_node] < INF_WEIGHT) {\n        int temp_path[MAX_NODES_DEFAULT];\n        int count = 0;\n        int curr = end_node;\n        while (curr != -1) {\n            temp_path[count++] = curr;\n            curr = parent[curr];\n        }\n\n        result.path = (int*)malloc(sizeof(int) * count);\n        result.path_length = count;\n        for (int i = 0; i < count; i++) {\n            result.path[i] = temp_path[count - 1 - i];\n        }\n\n        result.total_distance = dist[end_node];\n        // Estimate time in minutes assuming average emergency speed 60 km/h\n        result.total_time_minutes = (dist[end_node] / 60.0) * 60.0;\n    }\n\n    clock_t end_clk = clock();\n    result.execution_time_ms = ((double)(end_clk - start_clk) / CLOCKS_PER_SEC) * 1000.0;\n\n    free(dist);\n    free(parent);\n    free_hashset(visited);\n    free_minheap(heap);\n\n    return result;\n}\n\n// ----------------------------------------------------\n// MULTI-LOCATION EMERGENCY DISPATCH QUERY\n// ----------------------------------------------------\n\nMultiDispatchResult find_nearest_emergency_dispatch(GraphHashMap* graph, int* dispatch_nodes, int dispatch_count, int incident_node, VehicleType vehicle) {\n    MultiDispatchResult best_dispatch;\n    best_dispatch.best_dispatch_node = -1;\n    best_dispatch.target_incident_node = incident_node;\n    best_dispatch.route.total_distance = INF_WEIGHT;\n\n    for (int i = 0; i < dispatch_count; i++) {\n        RouteResult route = dijkstra_shortest_path(graph, dispatch_nodes[i], incident_node, vehicle);\n        if (route.path_length > 0 && route.total_distance < best_dispatch.route.total_distance) {\n            if (best_dispatch.best_dispatch_node != -1) {\n                free(best_dispatch.route.path);\n            }\n            best_dispatch.best_dispatch_node = dispatch_nodes[i];\n            best_dispatch.route = route;\n        } else {\n            if (route.path) free(route.path);\n        }\n    }\n\n    return best_dispatch;\n}\n\n// ----------------------------------------------------\n// SCALE TEST & BENCHMARK (5,000+ NODES, 10,000+ ROADS)\n// ----------------------------------------------------\n\nvoid benchmark_5000_nodes_test(void) {\n    printf(\"\\n===========================================================\\n\");\n    printf(\"   SMART-CITY ROUTING BENCHMARK (5,000 NODES / 10,000 ROADS)\\n\");\n    printf(\"===========================================================\\n\");\n    printf(\"Initializing synthetic urban grid & road network...\\n\");\n\n    GraphHashMap* graph = create_graph();\n    int NUM_INTERSECTIONS = 5000;\n    int NUM_ROADS = 12000;\n\n    // Generate intersections with grid-like coordinate clusters\n    for (int i = 0; i < NUM_INTERSECTIONS; i++) {\n        double x = (i % 71) * 0.5 + ((double)rand() / RAND_MAX) * 0.1;\n        double y = (i / 71) * 0.5 + ((double)rand() / RAND_MAX) * 0.1;\n        char name[32];\n        snprintf(name, sizeof(name), \"Int_%d\", i);\n        add_node(graph, i, name, x, y);\n    }\n\n    // Connect adjacent nodes to form a realistic connected city web\n    srand(42);\n    for (int i = 0; i < NUM_INTERSECTIONS; i++) {\n        int neighbor1 = (i + 1) % NUM_INTERSECTIONS;\n        int neighbor2 = (i + 71) % NUM_INTERSECTIONS;\n        int neighbor3 = (i + rand() % 50) % NUM_INTERSECTIONS;\n\n        double dist1 = 1.0 + ((double)rand() / RAND_MAX) * 3.0;\n        double dist2 = 1.5 + ((double)rand() / RAND_MAX) * 4.0;\n        double dist3 = 2.0 + ((double)rand() / RAND_MAX) * 5.0;\n\n        add_edge(graph, i, neighbor1, dist1, 1.0);\n        add_edge(graph, neighbor1, i, dist1, 1.0);\n\n        add_edge(graph, i, neighbor2, dist2, 1.0);\n        add_edge(graph, neighbor2, i, dist2, 1.0);\n\n        add_edge(graph, i, neighbor3, dist3, 1.2);\n    }\n\n    printf(\"Graph successfully loaded:\\n\");\n    printf(\" - Total Intersections: %d\\n\", graph->total_nodes);\n    printf(\" - Total Road Segments: %d\\n\", graph->total_edges);\n\n    // Run Dijkstra benchmark across long-distance endpoints\n    int start_node = 12;\n    int end_node = 4890;\n\n    printf(\"\\nExecuting Dijkstra Shortest Path query (Start: %d -> End: %d)...\\n\", start_node, end_node);\n    \n    RouteResult res = dijkstra_shortest_path(graph, start_node, end_node, VEHICLE_AMBULANCE);\n\n    printf(\"\\n--- BENCHMARK RESULTS ---\\n\");\n    printf(\" Execution Time     : %.3f ms (Target Constraint: < 200.000 ms)\\n\", res.execution_time_ms);\n    printf(\" Latency Status     : %s\\n\", res.execution_time_ms < 200.0 ? \"PASSED (SUPERIOR PERFORMANCE)\" : \"FAILED\");\n    printf(\" Path Length        : %d intersections\\n\", res.path_length);\n    printf(\" Total Route Cost   : %.2f km\\n\", res.total_distance);\n    printf(\" Visited Nodes      : %d / %d (O(1) HashSet lookup)\\n\", res.nodes_visited_count, graph->total_nodes);\n    printf(\" Heap Operations    : %d (O(log V) Min-Heap operations)\\n\", res.heap_operations_count);\n\n    // Test O(1) Dynamic Traffic Update\n    printf(\"\\nSimulating Live Dynamic Traffic Incident (Road 12 -> 13 accident bottleneck x10.0 multiplier)...\\n\");\n    clock_t t_up1 = clock();\n    update_traffic_condition(graph, 12, 13, 10.0);\n    clock_t t_up2 = clock();\n    double update_ms = ((double)(t_up2 - t_up1) / CLOCKS_PER_SEC) * 1000.0;\n    printf(\" HashMap Dynamic Edge Update Latency: %.5f ms (O(1) complexity)\\n\", update_ms);\n\n    // Re-route after dynamic traffic update\n    RouteResult res_reroute = dijkstra_shortest_path(graph, start_node, end_node, VEHICLE_AMBULANCE);\n    printf(\" Re-routed Distance : %.2f km (Re-calculated in %.3f ms)\\n\", res_reroute.total_distance, res_reroute.execution_time_ms);\n\n    if (res.path) free(res.path);\n    if (res_reroute.path) free(res_reroute.path);\n    free_graph(graph);\n}\n\n// ----------------------------------------------------\n// DATA STRUCTURE COMPARISON DISPLAY\n// ----------------------------------------------------\n\nvoid print_data_structure_comparison(void) {\n    printf(\"\\n===========================================================\\n\");\n    printf(\"   DATA STRUCTURE & ALGORITHM EVALUATION MATRIX           \\n\");\n    printf(\"===========================================================\\n\");\n    printf(\"%-20s | %-18s | %-18s | %-15s\\n\", \"Metric / Feature\", \"Adjacency Matrix\", \"Adjacency List (Hash)\", \"Floyd-Warshall\");\n    printf(\"-----------------------------------------------------------------------------------\\n\");\n    printf(\"%-20s | %-18s | %-18s | %-15s\\n\", \"Memory (V=5000)\", \"O(V^2) ~200 MB\", \"O(V+E) ~1.5 MB\", \"O(V^2) ~200 MB\");\n    printf(\"%-20s | %-18s | %-18s | %-15s\\n\", \"Edge Lookup\", \"O(1)\", \"O(1) Avg (HashMap)\", \"O(1)\");\n    printf(\"%-20s | %-18s | %-18s | %-15s\\n\", \"Dijkstra Time\", \"O(V^2) ~250ms\", \"O((V+E)logV) ~5ms\", \"N/A\");\n    printf(\"%-20s | %-18s | %-18s | %-15s\\n\", \"Dynamic Traffic Update\", \"O(1)\", \"O(1) Avg (HashMap)\", \"O(V^3) recompute\");\n    printf(\"%-20s | %-18s | %-18s | %-15s\\n\", \"Scalability (V=5000)\", \"Poor\", \"Optimal\", \"Impossible (O(V^3))\");\n    printf(\"-----------------------------------------------------------------------------------\\n\");\n}\n\n// ----------------------------------------------------\n// MAIN ENTRY POINT\n// ----------------------------------------------------\n\nint main(int argc, char* argv[]) {\n    printf(\"===============================================================\\n\");\n    printf(\"   SMART-CITY EMERGENCY VEHICLE ROUTING SYSTEM (C ENGINE)      \\n\");\n    printf(\"   Dijkstra + Min-Heap | HashMap Adjacency | Visited HashSet   \\n\");\n    printf(\"===============================================================\\n\");\n\n    if (argc > 1 && strcmp(argv[1], \"--benchmark\") == 0) {\n        benchmark_5000_nodes_test();\n        return 0;\n    }\n\n    if (argc > 1 && strcmp(argv[1], \"--matrix\") == 0) {\n        print_data_structure_comparison();\n        return 0;\n    }\n\n    // Default sample execution\n    GraphHashMap* graph = create_graph();\n    \n    // Add sample city intersections\n    add_node(graph, 0, \"Central Hospital\", 10.0, 20.0);\n    add_node(graph, 1, \"North Fire Depot\", 15.0, 45.0);\n    add_node(graph, 2, \"Downtown Square\", 30.0, 25.0);\n    add_node(graph, 3, \"East Suburb Junction\", 50.0, 30.0);\n    add_node(graph, 4, \"South Highway Interchange\", 25.0, 5.0);\n    add_node(graph, 5, \"West Commercial Center\", 5.0, 10.0);\n\n    // Add weighted roads\n    add_edge(graph, 0, 2, 4.5, 1.0);\n    add_edge(graph, 2, 0, 4.5, 1.0);\n    add_edge(graph, 1, 2, 6.0, 1.0);\n    add_edge(graph, 2, 3, 5.0, 2.5); // Heavy traffic\n    add_edge(graph, 0, 5, 2.0, 1.0);\n    add_edge(graph, 5, 4, 3.5, 1.0);\n    add_edge(graph, 4, 3, 4.0, 1.0);\n\n    printf(\"\\nSample Graph Initialized (6 Intersections, 7 Directed Roads).\\n\");\n    printf(\"Calculating optimal route for Ambulance from Central Hospital (Node 0) to East Suburb Junction (Node 3)...\\n\");\n\n    RouteResult route = dijkstra_shortest_path(graph, 0, 3, VEHICLE_AMBULANCE);\n\n    printf(\"\\nRoute Results:\\n\");\n    printf(\" - Total Distance: %.2f km\\n\", route.total_distance);\n    printf(\" - Estimated Travel Time: %.2f mins\\n\", route.total_time_minutes);\n    printf(\" - Intersections in path: \");\n    for (int i = 0; i < route.path_length; i++) {\n        printf(\"%d%s\", route.path[i], (i == route.path_length - 1) ? \"\\n\" : \" -> \");\n    }\n    printf(\" - Query Latency: %.4f ms\\n\", route.execution_time_ms);\n\n    if (route.path) free(route.path);\n\n    // Run full benchmark suite\n    benchmark_5000_nodes_test();\n    print_data_structure_comparison();\n\n    free_graph(graph);\n    return 0;\n}\n"}